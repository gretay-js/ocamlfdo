(executable
 (name ocamlfdo)
 (modules ocamlfdo elf_locations reorder wrapper cfg)
 (libraries
        owee
        core
        csvfields
        compiler-libs.common
        compiler-libs.bytecomp
        compiler-libs.optcomp)
 (preprocess (pps ppx_jane ppx_csv_conv)))


(alias (name DEFAULT) (deps ocamlfdo.ml.pp))
